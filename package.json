{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build_": "vite build && vite build --ssr",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@rollup/plugin-inject": "^5.0.5",
        "axios": "^1.7.4",
        "jquery": "^3.7.1",
        "laravel-vite-plugin": "^1.0",
        "vite": "^6.0",
        "vite-plugin-inspect": "^0.10.6"
    }
}
